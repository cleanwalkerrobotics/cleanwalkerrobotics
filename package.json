{
	"name": "cleanwalkerrobotics",
	"private": true,
	"packageManager": "pnpm@10.14.0",
	"scripts": {
		"check-demos": "node scripts/check-demo-registry.mjs",
		"check-manifests": "node scripts/check-manifests.mjs",
		"prebuild": "node scripts/check-manifests.mjs",
		"build": "turbo run build",
		"dev": "turbo run dev",
		"lint": "turbo run lint",
		"check": "biome check .",
		"check:fix": "biome check --write .",
		"clean": "turbo run clean"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.0",
		"playwright": "^1.58.2",
		"turbo": "^2.4.0",
		"typescript": "^5.7.0"
	},
	"dependencies": {
		"three": "^0.182.0"
	}
}
