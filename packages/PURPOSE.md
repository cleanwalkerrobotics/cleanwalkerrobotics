# packages — Purpose

Shared TypeScript packages consumed by apps and services.

**Owner:** cw-software
**What belongs here:** Shared type definitions (types/), database schema and utilities (db/), UI component library (ui/), shared config (config/).
**What does NOT belong here:** App-specific code (→ apps/), API routes (→ services/), standalone tools (→ scripts/).
