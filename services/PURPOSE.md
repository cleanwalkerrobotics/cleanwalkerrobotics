# services — Purpose

Backend API services for the CleanWalker platform.

**Owner:** cw-software
**What belongs here:** Hono API routes, backend business logic, server-side integrations, webhook handlers.
**What does NOT belong here:** Frontend code (→ apps/), shared types/schemas (→ packages/types/), database schema (→ packages/db/).
