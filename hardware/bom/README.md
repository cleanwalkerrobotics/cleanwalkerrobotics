# Bill of Materials

BOM management for CleanWalker hardware.

Use `bom-template.csv` as the base template for tracking components across all PCB designs. Each board should maintain its own BOM file derived from this template.
